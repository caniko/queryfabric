[package]
name = "queryfabric-types"
description = "Validated string newtypes and portable enum types"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = { workspace = true }
